{
  "name": "gatekid-rpg",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "prepare": "husky install"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "typescript": "^5.0.2",
    "vite": "^4.3.9",
    "husky": "^8.0.0"
  },
  "dependencies": {
    "phaser": "^3.60.0"
  }
}
